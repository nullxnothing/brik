{
  "name": "brick",
  "private": true,
  "version": "0.1.0",
  "description": "BRICK — browser-native development platform for Solana",
  "scripts": {
    "dev": "pnpm --filter @brick/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "bakeoff": "pnpm --filter @brick/bakeoff start"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  }
}
